<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<!--Targets to write-->
	<targets>
		<target name="allfile" xsi:type="File" archiveOldFileOnStartup='true'
				fileName="c:\DemoLogs\nlog-all-${shortdate}.log"/>
	</targets>
	<!--rules to map logger name to target-->
	<rules>
		<!--All logs including from Microsofts-->
		<logger name="*" minlevel="ERROR" writeTo="allfile" />
	</rules>
</nlog>

<!-- setting go to nLog.config file properties Copy to out directory :Copy if newer-->
<!--install NLog.Web.Asp.NetCore-->